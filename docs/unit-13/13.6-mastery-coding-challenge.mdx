---
id: mastery-coding-challenge
title: "Unit 13 Project Stage: Complete Modular TeleOp"
sidebar_label: "Unit 13 · Coding Challenge"
sidebar_position: 10
---

import MarkComplete from '@site/src/components/HomepageFeatures/MarkComplete';
import MasterySimulator from '@site/src/components/MasterySimulator';

# Stage 12: Complete Modular TeleOp

The shared project adds `PoweredMechanism.java` and `RobotHardware.java`. This stage composes Drivetrain, Intake, Transfer, Launcher, and ArtifactSensors into the runnable DECODE TeleOp. Lift remains in the general lesson examples and is not part of this robot.

## Your Task

1. Put shared motor state, setup, `setPower()`, and `stop()` behavior in `PoweredMechanism`.
2. Reuse it from powered subsystem classes with `extends` and `@Override` where behavior differs.
3. Compose every subsystem in `RobotHardware`.
4. Initialize all subsystems through `robot.init(hardwareMap)` and update non-blocking state through `robot.update()`.
5. Stop every subsystem through `robot.stopAll()`.
6. Finish `CompetitionTeleOp` using only the RobotHardware public interface, with no raw motor, servo, or IMU access.

The OpMode should read like coordination code. A passing snapshot becomes the reference that the advanced autonomous stage reuses.

## Coding Challenge Simulator

The simulator turns the completed project into a KG-SFR DECODE driver-control challenge. Start in **Practice** to see the predicted artifact trajectory and use a replenishing supply. Switch to **Match** for a deterministic 120-second run with 18 alliance-side artifacts and no trajectory preview.

The three-artifact robot capacity and two-minute TeleOp clock follow the [DECODE Competition Manual](https://ftc-resources.firstinspires.org/ftc/archive/2026/game/cm-html/DECODE_Competition_Manual_TU32.htm).

The visible wheels, intake, transfer, and flywheel follow measured hardware output from your code. A fixed open-loop launcher power will therefore lose speed and change the shot arc as the virtual battery drains; a reachable `setVelocity()` target with PIDF stays much more consistent.

- Left stick: field-relative forward and sideways motion
- Right stick X: rotation
- Y: toggle precision mode
- X: reset heading
- Right bumper: intake and transfer forward
- Left bumper: reverse both mechanisms
- Right trigger: proportional flywheel velocity
- A: launch one ready artifact on the press edge

<MasterySimulator unit={13} project />

<MarkComplete
  lessonId="unit-13/mastery-coding-challenge"
  completesUnit="unit-13"
  nextUnit="/docs/unit-14"
  nextUnitName="Unit 14: Computer Vision"
/>
